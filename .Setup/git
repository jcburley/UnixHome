# sh, source via "."

export GITBASE=git@github.com:jcburley

git config --global user.email "james-github@burleyarch.com"
git config --global user.name "James Craig Burley"
git config --global push.default simple

ln -sv ~/github/UnixHome/.emacs.d/init.el ~/.emacs.d/init.el
